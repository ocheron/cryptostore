# ~*~ auto-generated by haskell-ci with config : 9f7d8ffa648de6f45c456dc146946f0db746469ef396f7c9755bd18b48159a96 ~*~

# Caching so the next build will be fast too.
cache:
  directories:
  - $HOME/.ghc
  - $HOME/.stack
  - $HOME/.local

language: generic
os: linux

jobs:
  include:
  - { env: BUILD=stack RESOLVER=ghc-8.0, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.2, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.4, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.6, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.8, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.10, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-8.10, addons: { apt: { packages: [ libgmp-dev ] } }, os: osx }
  - { env: BUILD=stack RESOLVER=ghc-9.0, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=stack RESOLVER=ghc-9.2, addons: { apt: { packages: [ libgmp-dev ] } } }
  - { env: BUILD=hlint }
  - { env: BUILD=weeder, addons: { apt: { packages: [ libgmp-dev ] } } }
  allow_failures:
  - { env: BUILD=hlint }
  - { env: BUILD=weeder, addons: { apt: { packages: [ libgmp-dev ] } } }

install:
  - export PATH=$HOME/.local/bin:$HOME/.cabal/bin:$PATH
  - mkdir -p ~/.local/bin
  - |
    case "$BUILD" in
      stack|weeder)
        if [ `uname` = "Darwin" ]
        then
          travis_retry curl -L https://www.stackage.org/stack/osx-x86_64 | tar xz --strip-components=1 --include '*/stack' -C ~/.local/bin
        else
          travis_retry curl -L https://www.stackage.org/stack/linux-x86_64 | tar xz --wildcards --strip-components=1 -C ~/.local/bin '*/stack'
        fi
      ;;
    cabal)
      ;;
    esac

script:
- |
  set -ex
  if [ "x${RUNTEST}" = "xfalse" ]; then exit 0; fi
  case "$BUILD" in
    stack)
      # create the build stack.yaml
      case "$RESOLVER" in
      ghc-8.0)
        echo "{ resolver: lts-9.21, packages: [ '.' ], extra-deps: [ basement-0.0.7, foundation-0.0.20, memory-0.14.18, cryptonite-0.26, x509-1.7.5 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.2)
        echo "{ resolver: lts-11.22, packages: [ '.' ], extra-deps: [ basement-0.0.7, foundation-0.0.20, memory-0.14.18, cryptonite-0.26, x509-1.7.5 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.4)
        echo "{ resolver: lts-12.26, packages: [ '.' ], extra-deps: [ cryptonite-0.26 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.6)
        echo "{ resolver: lts-14.27, packages: [ '.' ], extra-deps: [ cryptonite-0.26 ], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.8)
        echo "{ resolver: lts-16.31, packages: [ '.' ], extra-deps: [], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-8.10)
        echo "{ resolver: lts-18.28, packages: [ '.' ], extra-deps: [], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-9.0)
        echo "{ resolver: lts-19.2, packages: [ '.' ], extra-deps: [], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      ghc-9.2)
        echo "{ resolver: nightly-2022-04-09, packages: [ '.' ], extra-deps: [], flags: {} }" > stack.yaml
        stack --no-terminal build --install-ghc --coverage --test --bench --no-run-benchmarks --haddock --no-haddock-deps
        ;;
      esac
      ;;
    hlint)
      curl -sL https://raw.github.com/ndmitchell/hlint/master/misc/travis.sh | sh -s . --cpp-define=__GLASGOW_HASKELL__=800 --cpp-define=x86_64_HOST_ARCH=1 --cpp-define=mingw32_HOST_OS=1
      ;;
    weeder)
      stack --no-terminal build --install-ghc --ghc-options="-ddump-to-file -ddump-hi" --test --no-run-tests --bench --no-run-benchmarks
      curl -sL https://raw.github.com/ndmitchell/weeder/master/misc/travis.sh | sh -s .
      ;;
  esac
  set +ex

